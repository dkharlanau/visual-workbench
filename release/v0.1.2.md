# Visual Workbench v0.1.2

This documentation patch makes the first useful rendering result explicit near the top of the README.

## Changed

- Added a copy-paste proof that validates and renders the committed synthetic handoff example.
- Linked the exact public gallery artifact and release-pinned deterministic walkthrough.
- Clarified that deterministic rendering does not certify source semantics or replace audience review.

## Compatibility

The v0.1 metadata language, CLI behavior, rendering fixtures, producer adapters, and golden SVG digest are unchanged from v0.1.1.

## Distribution

Run the [golden quickstart](https://github.com/dkharlanau/visual-workbench/blob/v0.1.2/docs/GOLDEN_QUICKSTART.md). The GitHub Release attaches a reproducible npm package tarball, deterministic source archive, and SHA-256 checksums. The package remains a source distribution and is not published to the npm registry.
